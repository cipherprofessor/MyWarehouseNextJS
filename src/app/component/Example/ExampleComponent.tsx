import styles from './ExampleComponent.module.scss';

const ExampleComponent = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>Hello, Gate Management!</h1>
    </div>
  );
};

export default ExampleComponent;
